{
  "name": "atlas-core",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de IA para gestão de pequenas empresas via WhatsApp",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}